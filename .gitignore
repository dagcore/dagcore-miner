# Binare compilate (vezi Makefile).
# dagtech-gpu-miner ramane versionat deocamdata, ca referinta.
dagcore-miner
dagcore-miner-cpu
dagtech-gpu-miner
*.exe
# Build-ul Windows (build/win) si kitul de copiat pe Windows (dist/windows).
build/
dist/
